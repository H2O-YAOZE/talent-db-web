{
  "name": "AI Talent Database",
  "image": "mcr.microsoft.com/devcontainers/python:3.11",
  "forwardPorts": [8080],
  "portsAttributes": {
    "8080": {
      "label": "Web App",
      "onAutoForward": "openBrowser"
    }
  },
  "containerEnv": {
    "TALENT_LLM_API_KEY": "${localEnv:TALENT_LLM_API_KEY}",
    "TALENT_SECRET_KEY": "${localEnv:TALENT_SECRET_KEY}"
  },
  "postCreateCommand": "pip install -r requirements.txt && mkdir -p ~/projects/talent-db/data ~/projects/talent-db/input/papers ~/projects/talent-db/input/resumes",
  "postStartCommand": "python server.py",
  "customizations": {
    "codespaces": {
      "openFiles": ["README.md"]
    }
  }
}
